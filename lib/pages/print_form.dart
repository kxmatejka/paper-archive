import 'package:flutter/material.dart';
import '../page_body.dart';

class PrintForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return PageBody(
      child: Text('awdawd'),
    );
  }
}
